import React from 'react';

const Timestamp = props => (
  <div>
    This is a placeholder for the Timestamp.
    <pre>
      <code>{JSON.stringify(props)}</code>
    </pre>
  </div>
);

export default Timestamp;